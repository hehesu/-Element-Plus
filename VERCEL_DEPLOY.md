# Vercel 部署 VitePress 文档详细指南

本指南将一步步教你如何使用 Vercel 部署你的组件库文档。

---

## 📋 前置准备

### 1. 确保代码已推送到 GitHub

```bash
# 检查当前 Git 状态
git status

# 如果还没有初始化 Git
git init
git add .
git commit -m "Initial commit"

# 在 GitHub 创建仓库后，关联远程仓库
git remote add origin https://github.com/你的用户名/你的仓库名.git
git branch -M main
git push -u origin main
```

### 2. 检查 Node.js 版本

Vercel 需要 Node.js，建议使用 18.x 或更高版本：

```bash
node --version  # 应该显示 v18.x 或更高
```

---

## 🚀 方法一：通过网页界面部署（推荐，最简单）

### 第一步：注册/登录 Vercel

1. 访问 https://vercel.com
2. 点击右上角的 **"Sign Up"** 或 **"Log In"**
3. 推荐使用 **GitHub 账号登录**（这样可以直接导入 GitHub 仓库）

### 第二步：创建新项目

1. 登录后，点击 **"Add New..."** → **"Project"**
2. 如果是第一次使用，会提示导入 GitHub 仓库
3. 在仓库列表中找到你的项目，点击 **"Import"**

### 第三步：配置项目

Vercel 会自动检测项目类型，但我们需要手动配置 VitePress：

#### 🔧 重要配置项：

1. **Framework Preset**
   - 选择：**Vite** 或 **Other**

2. **Root Directory**
   - 设置为：`./` （项目根目录）

3. **Build Command**
   - 设置为：`npm run docs:build`
   - 这是构建文档的命令

4. **Output Directory**
   - 设置为：`docs/.vitepress/dist`
   - 这是 VitePress 构建输出目录

5. **Install Command**
   - 设置为：`npm install`
   - 或者留空（使用默认）

#### 📝 完整配置示例：

```
Framework Preset: Other
Root Directory: ./
Build Command: npm run docs:build
Output Directory: docs/.vitepress/dist
Install Command: npm install
```

### 第四步：环境变量（如果需要）

如果你的项目需要环境变量，可以在这里添加。对于 VitePress 文档，通常不需要。

### 第五步：部署

1. 检查所有配置无误后
2. 点击 **"Deploy"** 按钮
3. 等待构建完成（通常 2-5 分钟）

### 第六步：获取访问地址

部署成功后，你会看到：
- ✅ 部署成功提示
- 🔗 访问地址：`https://你的项目名.vercel.app`

点击地址即可访问你的文档！

---

## 💻 方法二：通过 CLI 部署（适合开发者）

### 第一步：安装 Vercel CLI

```bash
npm install -g vercel
```

### 第二步：登录 Vercel

```bash
vercel login
```

这会打开浏览器，让你登录 Vercel 账号。

### 第三步：创建 Vercel 配置文件（可选但推荐）

在项目根目录创建 `vercel.json` 文件：

```json
{
  "buildCommand": "npm run docs:build",
  "outputDirectory": "docs/.vitepress/dist",
  "devCommand": "npm run docs:dev",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### 第四步：部署

```bash
# 在项目根目录执行
vercel

# 按提示操作：
# ? Set up and deploy "~/项目路径"? [Y/n] y
# ? Which scope do you want to deploy to? 选择你的账号
# ? Link to existing project? [y/N] n
# ? What's your project's name? element-ui-docs
# ? In which directory is your code located? ./
```

### 第五步：生产环境部署

```bash
vercel --prod
```

---

## ⚙️ 重要配置说明

### 1. 修改 base 路径

在 `docs/.vitepress/config.ts` 中，根据部署路径修改 `base`：

#### 情况 A：部署在根路径（推荐）

如果你使用 Vercel 的默认域名（`your-project.vercel.app`）：

```typescript
export default defineConfig({
  base: '/',  // 修改这里
  // ... 其他配置
})
```

#### 情况 B：部署在子路径

如果你使用自定义域名或部署在子路径：

```typescript
export default defineConfig({
  base: '/element-ui/',  // 保持当前配置
  // ... 其他配置
})
```

**注意**：如果修改了 `base`，需要重新构建：
```bash
npm run docs:build
```

### 2. 创建 vercel.json 配置文件

在项目根目录创建 `vercel.json`：

```json
{
  "buildCommand": "npm run docs:build",
  "outputDirectory": "docs/.vitepress/dist",
  "devCommand": "npm run docs:dev",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

这个配置文件的作用：
- `buildCommand`: 指定构建命令
- `outputDirectory`: 指定输出目录
- `rewrites`: 处理 SPA 路由（VitePress 需要）

---

## 🔄 自动部署配置

### 每次推送代码自动部署

一旦通过网页部署成功，Vercel 会自动：

1. ✅ 监听 GitHub 仓库的推送
2. ✅ 自动触发构建
3. ✅ 自动部署新版本

**你只需要**：
```bash
git add .
git commit -m "更新文档"
git push
```

Vercel 会自动部署！🚀

### 配置自动部署分支

1. 在 Vercel 项目设置中
2. 进入 **Settings** → **Git**
3. 选择要自动部署的分支（通常是 `main` 或 `master`）

---

## 🌐 自定义域名配置

### 第一步：购买域名（可选）

如果你有自己的域名，可以绑定到 Vercel。

### 第二步：添加域名

1. 在 Vercel 项目页面
2. 点击 **Settings** → **Domains**
3. 输入你的域名（如：`docs.yourdomain.com`）
4. 按照提示配置 DNS

### 第三步：配置 DNS

Vercel 会提供 DNS 配置说明，通常需要添加：

```
类型: CNAME
名称: docs（或 @）
值: cname.vercel-dns.com
```

### 第四步：等待生效

DNS 配置通常需要几分钟到几小时生效。

---

## 📊 部署后检查清单

部署完成后，检查以下内容：

- [ ] ✅ 文档首页可以正常访问
- [ ] ✅ 组件页面可以正常显示
- [ ] ✅ 组件演示（Preview）可以正常运行
- [ ] ✅ 导航菜单可以正常跳转
- [ ] ✅ 静态资源（图片、CSS、JS）正常加载
- [ ] ✅ 移动端显示正常（响应式）

---

## 🐛 常见问题解决

### 问题 1：构建失败

**错误信息**：`Build failed` 或 `npm install` 失败

**解决方案**：
1. 检查 Node.js 版本（需要 18+）
2. 在 Vercel 设置中指定 Node.js 版本：
   - Settings → General → Node.js Version → 选择 `18.x`
3. 检查 `package.json` 中的依赖是否正确

### 问题 2：页面显示空白或 404

**原因**：`base` 路径配置错误

**解决方案**：
1. 检查 `docs/.vitepress/config.ts` 中的 `base` 配置
2. 如果使用 Vercel 默认域名，设置为 `base: '/'`
3. 重新部署

### 问题 3：组件演示不显示

**原因**：路径别名或依赖问题

**解决方案**：
1. 检查 `docs/.vitepress/config.ts` 中的别名配置：
   ```typescript
   vite: {
     resolve: {
       alias: {
         '@': fileURLToPath(new URL('../../src', import.meta.url))
       }
     }
   }
   ```
2. 确保所有依赖都在 `package.json` 中

### 问题 4：路由跳转 404

**原因**：缺少路由重写规则

**解决方案**：
确保 `vercel.json` 中有 `rewrites` 配置：
```json
{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
```

### 问题 5：构建时间过长

**原因**：依赖安装或构建过程慢

**解决方案**：
1. 使用 Vercel 的缓存功能
2. 检查是否有不必要的依赖
3. 考虑使用 `npm ci` 而不是 `npm install`（更快）

---

## 📝 部署命令速查

```bash
# 本地预览文档
npm run docs:dev

# 本地构建文档
npm run docs:build

# 本地预览构建后的文档
npm run docs:preview

# Vercel CLI 登录
vercel login

# Vercel 部署（预览）
vercel

# Vercel 生产环境部署
vercel --prod

# 查看部署日志
vercel logs
```

---

## 🎯 最佳实践

### 1. 使用环境变量管理配置

如果有不同环境的配置，可以在 Vercel 中设置环境变量：
- Settings → Environment Variables

### 2. 启用构建缓存

Vercel 会自动缓存 `node_modules`，但可以手动优化：
- 使用 `package-lock.json` 锁定依赖版本
- 避免在构建时安装不必要的依赖

### 3. 监控部署状态

Vercel 提供了：
- 部署历史记录
- 构建日志
- 性能监控

### 4. 分支预览

Vercel 可以为每个分支创建预览部署：
- 创建 Pull Request 时自动部署预览
- 方便测试和审查

---

## 🎉 部署成功后的操作

1. **分享链接**：将 Vercel 提供的链接分享给团队成员
2. **添加到 README**：在 README.md 中添加文档链接
3. **配置自动部署**：确保每次推送代码都自动部署
4. **设置自定义域名**（可选）：使用自己的域名

---

## 📚 相关资源

- Vercel 官方文档：https://vercel.com/docs
- VitePress 部署指南：https://vitepress.dev/guide/deploy
- Vercel 状态页面：https://www.vercel-status.com

---

## 💡 总结

使用 Vercel 部署 VitePress 文档非常简单：

1. ✅ 推送到 GitHub
2. ✅ 在 Vercel 导入项目
3. ✅ 配置构建命令和输出目录
4. ✅ 点击部署

完成后，你的文档就上线了！🚀

有任何问题，可以查看 Vercel 的构建日志来排查。

